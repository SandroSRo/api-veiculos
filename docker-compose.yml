version: "3"
services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    environment:
      - MONGO_URI=mongodb://mongo:27017/veichules
    depends_on:
      - mongo
  mongo:
    image: mongo
    ports:
      - "27017:27017"
